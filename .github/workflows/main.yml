name: Candlestick MYSQL Pipeline

on: 
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'

jobs:
    build_image_job:
        name: Docker Containerization
        #needs: unit_test_job
        if: github.ref == 'refs/head/feature'
        runs-on: self-hosted
        steps:
            - name: Checkout
              uses: actions/checkout@v5

            - name: Docker Login
              uses: docker/login-action@v2.2.0
              with:
                username: ${{ vars.DOCKER_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD}}

            - name: Docker Build
              uses: docker/build-push-action@v4
              with:
                context: .
                push: false
                tags: ${{ vars.DOCKER_USERNAME }}/${{ vars.IMAGE_NAME }}:latest

            - name: Docker Image Testing
              run: |
                  docker run --name mysql-app -itd \
                      --env=MYSQL_ROOT_PASSWORD=${{ vars.MYSQL_PASS }}\
                      -p 3306:3306 \
                      ${{ vars.DOCKER_USERNAME }}/${{ vars.IMAGE_NAME }}:latest

                  sleep 30s
                  #curl http://localhost:3000/
                  docker stop mysql-app && docker rm mysql-app

            - name: Docker Push
              uses: docker/build-push-action@v4
              with:
                context: .
                push: true
                tags: ${{ vars.DOCKER_USERNAME }}/${{ vars.IMAGE_NAME }}:latest


            #- name: Docker Logout
            #  run: sudo docker logout https://docker.io
    deploy_image_job:
        name: Deploy Docker Image
        #if: github.ref == 'refs/head/main'
        #needs: build_image_job
        outputs:
          APP_INGRESS_URL: ${{ steps.set-loadbalancer-host-id.outputs.APP_INGRESS_HOST}}
        runs-on: self-hosted
        steps:
            - name: Checkout
              uses: actions/checkout@v5
              
            - name: Install Kubectl CLI
              uses: azure/setup-kubectl@v3
              with:
                version: 'v1.33.6'

            - name: Set Kubeconfig
              uses: azure/k8s-set-context@v3
              with:
                method: kubeconfig
                kubeconfig: ${{ secrets.KUBECONFIG }}
                
            - name: Fetch Kubernetes CLuster Details
              run: |
                  kubectl version
                  echo ............................
                  kubectl get nodes

            - name: Replace Token in Manifest files
              uses: cschleiden/replace-tokens@v1
              with:
                tokenPrefix: '_{_'
                tokenSuffix: '_}_'
                files: '["kubernetes/development/*.yaml"]'
              env:
                NAMESPACE: ${{ vars.NAMESPACE }}
                REPLICAS: ${{ vars.REPLICAS}}
                IMAGE: ${{ vars.DOCKER_USERNAME }}/${{ vars.IMAGE_NAME }}:latest
                MYSQL_PASS: ${{ vars.MYSQL_PASS }}

            - name: Check Files
              run: |
                  cat kubernetes/development/*.yaml

            - name: Deploy to Dev Env
              run: |
                  kubectl apply -f kubernetes/development

            - name: Set App Ingress Host URL
              id: set-loadbalancer-host-id
              run: |
                  #echo "APP_INGRESS_HOST=$(kubectl -n ${{ vars.NAMESPACE }} get ingress -o json | jq -r '.items[] | .spec.rules[] | .host')" >> "$GITHUB_OUTPUT"
                  #echo $APP_INGRESS_HOST
    integration_test_job:
        name: Dev Integration Testing
        #if: github.ref == 'refs/head/main'
        needs: deploy_image_job
        runs-on: self-hosted
        steps:
            - name: Test URL Output using cURL
              env:
                URL: ${{ needs.deploy_image_job.outputs.APP_INGRESS_URL }}
              run: |
                  echo $URL
                  echo "................................"
                  curl http://$URL
